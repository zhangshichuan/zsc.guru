import { Clock } from '../components/Clock'
import HelloWorld from './HelloWorld.md'

# MDX 示例

这是一个使用 MDX 的简单示例，展示了如何结合 Markdown 和 React 组件。

## 嵌入一个 React 组件

在 MDX 文件中，你可以直接嵌入 React 组件：

<Clock />

## 或者再嵌入其他 Markdown 文件

> 各种混合语法都行

> <HelloWorld />
